{
  "name": "hivecmd",
  "version": "1.0.0",
  "description": "Agent Swarm Intelligence CLI - One Command → Full Automation",
  "license": "MIT",
  "author": "HsinPu",
  "repository": "https://github.com/HsinPu/hivecmd",
  "keywords": ["ai", "agent", "swarm", "claude", "codex", "openclaw"],
  "python_requires": ">=3.10",
  "dependencies": {
    "typer": ">=0.12.0",
    "pydantic": ">=2.0.0",
    "rich": ">=13.0.0",
    "zeromq": ">=6.0.0"
  },
  "devDependencies": {
    "pytest": ">=7.0.0",
    "pytest-mock": ">=3.0.0"
  },
  "scripts": {
    "dev": "python -m hivecmd",
    "test": "pytest tests/"
  },
  "entry_points": {
    "console_scripts": [
      "hivecmd=hivecmd.__main__:app"
    ]
  }
}
